[alias]
  co = "checkout"
  pra = "pull --rebase --autostash"
  ame = "commit --amend --no-edit"
  poh = "push origin head"
  st = "status"
  frmain = "!git fetch origin main && git rebase origin/main"
